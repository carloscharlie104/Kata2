# Kata2
El objetivo es diseñar la clase del registro para que sea inmutable (es decir, nada de setters, solo constructor y getters). 
Luego simplemente montamos el proceso que lea el archivo, parsee las columnas y vaya instanciando un objeto por cada registro 
que encontremos.
